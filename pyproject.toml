[project]
name = "competitor-analysis-mas-v2"
version = "0.1.0"
description = "Competitive analysis multi-agent workflow with LangGraph platform entrypoints"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.28.0",
    "langgraph>=1.2.1",
    "langchain-core>=1.4.0",
    "langgraph-cli[inmem]>=0.4.26"
]
